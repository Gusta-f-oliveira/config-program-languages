{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "windows": {
                "command": "cmd",
                "args": [
                    "/C",
                    "if not exist \"${workspaceFolder}\\bin\" mkdir \"${workspaceFolder}\\bin\" & if not exist \"${workspaceFolder}\\temp\" mkdir \"${workspaceFolder}\\temp\" & nasm -f win64 -g \"${file}\" -o \"${workspaceFolder}\\temp\\${fileBasenameNoExtension}.o\" && gcc -g \"${workspaceFolder}\\temp\\${fileBasenameNoExtension}.o\" -o \"${workspaceFolder}\\bin\\${fileBasenameNoExtension}.exe\""
                ]
            },
            "linux": {
                "command": "bash",
                "args": [
                    "-c",
                    "mkdir -p /tmp/vscode_bin && nasm -f elf64 -g \"${file}\" -o /tmp/vscode_bin/\"${fileBasenameNoExtension}.o\" && gcc -g -no-pie /tmp/vscode_bin/\"${fileBasenameNoExtension}.o\" -o /tmp/vscode_bin/\"${fileBasenameNoExtension}\""
                ]
            }
        }
    ]
}