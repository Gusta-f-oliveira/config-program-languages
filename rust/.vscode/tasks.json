{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build-rust",
            "type": "shell",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "presentation": {
                "reveal": "silent",
                "clear": true
            },
            "windows": {
                "command": "cmd",
                "args": [
                    "/c",
                    "mkdir \"${workspaceFolder}\\bin\" 2>nul & rustc -g \"${file}\" -o \"${workspaceFolder}\\bin\\${fileBasenameNoExtension}.exe\""
                ]
            },
            "linux": {
                "command": "bash",
                "args": [
                    "-c",
                    "mkdir -p /tmp/vscode_bin && rustc -g \"${file}\" -o /tmp/vscode_bin/\"${fileBasenameNoExtension}\""
                ]
            }
        }
    ]
}