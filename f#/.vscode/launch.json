{
    // Use o IntelliSense para saber mais sobre os atributos possíveis.
    // Focalizar para exibir as descrições dos atributos existentes.
    // Para obter mais informações, acesse: https://go.microsoft.com/fwlink/?linkid=830387
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug F# (Atual)",
            "type": "coreclr",
            "request": "launch",
            "preLaunchTask": "Build F# Universal",
            "args": [],
            "cwd": "${fileDirname}",
            "stopAtEntry": false,
            "console": "integratedTerminal",
            "windows": {
                "program": "${fileDirname}/build_output/app.dll"
            },
            "linux": {
                "program": "/tmp/vscode_bin/fsharp_build/${fileBasenameNoExtension}/bin/app.dll"
            }
        }
    ]
}